# Scripts

Command-line entry points:

```text
download_data.py
build_features.py
train_ml.py
train_dl.py
run_backtest.py
```

Scripts should call reusable functions in `src/` and should not duplicate core business logic.
